// import BN from 'bignumber.js/bignumber'

// export default class BigNumber extends BN {

    // ZERO
    // MAX_UINT_256

    // from(value)

    // parse(value)

    // sumEach(items, predicate)

    // scaleBy(decimals)

    // unscaleBy(decimals)

    // round()

// }